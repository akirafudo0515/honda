{
  "name": "honda-stores",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev htjwc.weebly.com --d1=DB --r2=FILES --persist-to=.wrangler/state",
    "migrate:local": "wrangler d1 execute DB --local --file=./schema.sql && wrangler d1 execute DB --local --file=./seed.sql",
    "migrate:remote": "wrangler d1 execute DB --remote --file=./schema.sql && wrangler d1 execute DB --remote --file=./seed.sql",
    "extract": "node scripts/extract-stores.js",
    "db:create": "wrangler d1 create honda-stores",
    "r2:create": "wrangler r2 bucket create honda-store-files"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
